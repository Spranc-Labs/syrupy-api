x-backend-common: &backend-common
  image: syrupy-api-dev:latest
  environment: &backend-common-env
    RAILS_ENV: development
    DATABASE_URL: postgresql://postgres:postgres@db/syrupy_development
    BUNDLE_PATH: /api-bundle
    BUNDLE_AUTO_INSTALL: "true"
    PIDFILE: /tmp/pids/server.pid
    GEM_HOME: /api-bundle
    HISTFILE: /root/hist/.bash_history
  tty: true
  stdin_open: true
  tmpfs:
    - /tmp/
    - /tmp/pids/
  volumes:
    - .:/app
    - "api_bundle:/api-bundle:rw,cached"
    - "api_history:/root/hist:rw,cached"
  depends_on:
    db:
      condition: service_healthy

services:
  api:
    <<: *backend-common
    build:
      context: .
      dockerfile: Dockerfile.dev
    command: [ "bundle", "exec", "rails", "server", "-b", "0.0.0.0", "-p", "3000" ]
    ports:
      - "3000:3000"
    shm_size: 4g

  good-job:
    <<: *backend-common
    command: [ "bundle", "exec", "good_job", "start" ]
    environment:
      <<: *backend-common-env
    healthcheck:
      test: "ps aux | grep good_job"
      interval: 10s
      timeout: 5s
      retries: 5

  db:
    image: postgres:14-alpine
    volumes:
      - "db_data:/var/lib/postgresql/data"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: syrupy_development
    ports:
      - "5433:5432"
    healthcheck:
      test: pg_isready -U postgres -h 127.0.0.1
      interval: 5s

volumes:
  db_data:
  api_bundle:
  api_history: 